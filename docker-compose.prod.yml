services:
  rag-server:
    environment:
      MNEMOS_MODE: deployed
      MNEMOS_AUTH_ENABLED: "true"
    volumes:
      - ./config/tenants.yaml:/data/config/tenants.yaml:ro

  watcher:
    profiles:
      - disabled
